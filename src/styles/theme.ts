
import { config } from 'process'
import configs from '../configs'
const theme = {
  colors: {
    background: configs.background,
    backgroundSecondary: configs.backgroundSecondary,
    textAlternative: configs.textAlternative,
    textSecondary: configs.textSecondary,
    text: configs.text,
    textOnDark:configs.textOnDark,
    primary:configs.primary,
    primaryShade: configs.primaryShade,
    secondary: configs.secondary,
    secondaryShade:configs.secondaryShade,
    primaryAlternative:configs.primaryAlternative
  }
}

export default theme
